# NOTE: values are plaintext here for local minikube use only.
# For a real cluster, create this with `kubectl create secret generic` or
# a sealed-secrets/external-secrets tool instead of committing values in git.
apiVersion: v1
kind: Secret
metadata:
  name: hotel-secrets
type: Opaque
stringData:
  DB_PASSWORD: "hotelvinayagam123"
  ADMIN_PASSWORD: "hotel123"
